import 'package:flutter/material.dart';
import 'package:monobox/features/order/domain/entities/gifts_scale_entity.dart';

class GiftScaleItems extends StatelessWidget {
  const GiftScaleItems({
    super.key,
    required this.loyalties,
  });

  final List<GiftsScaleEntity> loyalties;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
