import 'package:flutter/material.dart';

import 'add_child.dart';

class AddChildList extends StatelessWidget {
  const AddChildList({super.key});

  @override
  Widget build(BuildContext context) {
    return const AddChild();
  }
}
